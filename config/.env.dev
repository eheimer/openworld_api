# Development Environment Configuration
# This file contains non-sensitive default values for local development
# DO NOT put production credentials in this file

# Application Environment
NODE_ENV=dev

# Database Configuration
# SQLite is used for local development (no server required)
DB_TYPE=sqlite
DB_NAME=dev.sqlite

# MySQL Configuration (optional - uncomment to use MySQL instead of SQLite)
# DB_TYPE=mysql
# DB_HOST=localhost
# DB_PORT=3306
# DB_USERNAME=openworld
# DB_PASSWORD=your_dev_password
# DB_NAME=openworld_dev

# JWT Authentication Secret
# This is a development-only secret - NEVER use this in production
JWT_SECRET=dev-secret-key-not-for-production