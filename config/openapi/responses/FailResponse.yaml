description: Request failed
content:
  application/json:
    schema:
      type: object
      additionalProperties: false
      required:
        - error
      properties:
        success:
          nullable: true
          type: boolean
        error:
          $ref: '../schemas/_index.yaml#/Error'
